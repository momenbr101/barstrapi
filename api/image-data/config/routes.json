{
  "routes": [
    {
      "method": "GET",
      "path": "/image-data",
      "handler": "image-data.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-data/count",
      "handler": "image-data.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/image-data/:id",
      "handler": "image-data.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/image-data",
      "handler": "image-data.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/image-data/:id",
      "handler": "image-data.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/image-data/:id",
      "handler": "image-data.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
